/*
 * Copyright (c) 2020 Robert Hjortsholm Moeller
 */

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Optional', () {
    group('map', () {});
    group('flatMap', () {});
    group('ifPresent', () {});
    group('isPresent', () {});
    group('orElse', () {});
    group('orElseGet', () {});
    group('orThrow', () {});
  });
}
