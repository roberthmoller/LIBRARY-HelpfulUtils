import 'package:flutter_test/flutter_test.dart';

import 'control_flow/method.dart';
import 'control_flow/try.dart';
import 'date_time/date.dart';

void main() {
	group('Method', method);
  group('Date', date);
  group('Try', tryTest);
}
